# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Session Secret
# IMPORTANT: Change this to a random string in production
SESSION_SECRET=finding-sweetie-secret-key-change-in-production

# Database (SQLite - path is relative to project root)
# DB_PATH=./database/findingsweetie.db

# SMTP Configuration (for future email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# SMS Configuration (for future SMS notifications)
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# Production Settings
# For production deployment, set NODE_ENV=production
# and ensure SESSION_SECRET is a strong random value
